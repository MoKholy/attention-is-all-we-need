from setuptools import find_packages, setup

project_name = "Custom_Transformer_Implementation"
version_no = "0.1"
project_desc = "Attention is indeed all we need"
setup(
    name=project_name,
    version=version_no,
    description=project_desc,
    packages=find_packages(),
)
